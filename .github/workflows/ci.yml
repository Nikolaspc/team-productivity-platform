name: Enterprise CI Pipeline

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres_password
          POSTGRES_DB: productivity_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Setup Node.js Environment
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Project Dependencies
        run: cd bff-nestjs && npm install

      - name: Code Quality Audit (Lint)
        run: cd bff-nestjs && npm run lint

      - name: Initialize Database Client (Prisma)
        run: cd bff-nestjs && npx prisma generate

      - name: Execute Suite of Unit Tests
        run: cd bff-nestjs && npm run test

      - name: Execute End-to-End Tests
        env:
          DATABASE_URL: postgresql://postgres:postgres_password@localhost:5432/productivity_test?schema=public
          AT_SECRET: ${{ secrets.JWT_ACCESS_SECRET }}
          RT_SECRET: ${{ secrets.JWT_REFRESH_SECRET }}
          NODE_ENV: test
        run: cd bff-nestjs && npm run test:e2e

      - name: Final Build Production Artifact
        run: cd bff-nestjs && npm run build