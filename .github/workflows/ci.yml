name: Enterprise CI Pipeline

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

<<<<<<< HEAD
    defaults:
      run:
        working-directory: bff-nestjs

=======
>>>>>>> 9944cb2 (ci: implement enterprise github actions pipeline)
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres_password
          POSTGRES_DB: productivity_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
<<<<<<< HEAD
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: bff-nestjs/package-lock.json
=======
          node-version: "20"
          cache: "npm"
>>>>>>> 9944cb2 (ci: implement enterprise github actions pipeline)

      - name: Install dependencies
        run: npm ci

<<<<<<< HEAD
      - name: Run Lint (Style Check)
=======
      - name: Run Lint
>>>>>>> 9944cb2 (ci: implement enterprise github actions pipeline)
        run: npm run lint

      - name: Run Prisma Generate
        run: npx prisma generate

      - name: Run Unit Tests
        run: npm run test

      - name: Run E2E Tests
        env:
          DATABASE_URL: postgresql://postgres:postgres_password@localhost:5432/productivity_test?schema=public
          AT_SECRET: ${{ secrets.AT_SECRET }}
          RT_SECRET: ${{ secrets.RT_SECRET }}
          NODE_ENV: test
        run: npm run test:e2e

      - name: Build Application
        run: npm run build
